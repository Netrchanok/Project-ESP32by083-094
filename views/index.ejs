<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ข้อมูลสภาพอากาศ</title>
  <link rel="stylesheet" href="/css/main.css">
  <style>
    /* --- Search Bar --- */
    .search-input {
      padding: 12px 15px;
      font-size: 1em;
      border-radius: 20px;
      border: 1px solid #4a5568;
      width: 60%;
      max-width: 400px;
      transition: box-shadow 0.2s;
      background-color: #2d3748;
      color: #e2e8f0;
    }
    .search-input:focus {
      outline: none;
      border-color: #63b3ed;
      box-shadow: 0 0 0 3px rgba(99, 179, 237, 0.4);
    }
    .search-button {
      padding: 12px 20px;
      border-radius: 20px;
      border: none;
      background-color: #63b3ed;
      color: white;
      font-size: 1em;
      cursor: pointer;
      margin-left: 10px;
      transition: background-color 0.2s;
    }
    .search-button:hover {
      background-color: #4299e1;
    }

    /* --- General Card Style --- */
    .card {
      background: #2d3748;
      padding: 25px;
      margin-bottom: 20px;
      border-radius: 16px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      border: 1px solid #4a5568;
      transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
    }
    .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.3);
    }

    /* --- Section Headers --- */
    h2 {
      color: #63b3ed;
      border-bottom: 2px solid #4a5568;
      padding-bottom: 10px;
      margin-top: 40px;
      margin-bottom: 20px;
      font-size: 1.8em;
    }

    /* --- Sensor Data Section --- */
    .sensor-card {
      display: flex;
      justify-content: space-around;
      align-items: center;
      flex-wrap: wrap;
      gap: 20px;
      text-align: center;
      padding: 30px;
    }
    .sensor-item {
      font-size: 1.5em;
    }
    .sensor-item .icon {
      font-size: 2.5em;
      display: block;
      margin-bottom: 10px;
    }
    .sensor-item .label {
      font-size: 0.7em;
      color: #a0aec0;
    }
    .sensor-item .value {
      font-weight: bold;
      color: #fff;
    }
    .sensor-timestamp {
      width: 100%;
      text-align: center;
      margin-top: 20px;
      color: #a0aec0;
      font-size: 0.9em;
    }

    /* --- Weather Grid --- */
    .weather-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 20px;
    }

    /* --- Weather Card --- */
    .card-header {
      display: flex;
      align-items: center;
      gap: 15px;
      margin-bottom: 10px;
    }
    .card h3 {
      margin: 0;
      font-size: 1.5em;
      color: #e2e8f0;
    }
    .card p {
      margin: 8px 0;
      font-size: 1.1em;
      color: #cbd5e0;
    }
    .card small {
      color: #a0aec0;
      font-size: 0.9em;
      display: block;
      margin-top: 15px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🌤️ Weather Dashboard</h1>
    <div class="search-container">
      <form action="/" method="GET">
        <input type="text" name="q" placeholder="ค้นหาจังหวัด..." class="search-input" value="<%= typeof query !== 'undefined' ? query : '' %>">
        <button type="submit" class="search-button">🔍 ค้นหา</button>
      </form>
    </div>

    <!-- Sensor Data Section -->
    <% if (sensorData && sensorData.length > 0) { %>
      <div class="card sensor-card">
        <div class="sensor-item">
          <span class="icon">🌡️</span>
          <span class="label">อุณหภูมิ</span>
          <span class="value"><%= sensorData[0].temperature.toFixed(2) %> °C</span>
        </div>
        <div class="sensor-item">
          <span class="icon">💧</span>
          <span class="label">ความชื้น</span>
          <span class="value"><%= sensorData[0].humidity %> %</span>
        </div>
        <div class="sensor-timestamp">
          ข้อมูลจาก Sensor ล่าสุดเมื่อ: <%= sensorData[0].formattedTimestamp %>
        </div>
      </div>
    <% } %>

    <!-- Weather Data Section -->
    <% regions.forEach(region => { %>
      <% if (region.provinces.length > 0) { %>
        <h2><%= region._id %></h2>
        <div class="weather-grid">
          <% region.provinces.forEach(p => { %>
          <div class="card">
            <div class="card-header">
              <img src="https://openweathermap.org/img/wn/<%= p.icon %>@2x.png" alt="Weather icon" width="50" height="50">
              <h3><%= p.city %></h3>
            </div>
            <p><strong>อุณหภูมิ:</strong> <%= p.temp.toFixed(1) %> °C</p>
            <p><strong>ความชื้น:</strong> <%= p.humidity %> %</p>
            <p><strong>สภาพอากาศ:</strong> <%= p.weather %></p>
            <small>บันทึกเมื่อ: <%= p.formattedTimestamp %></small>
          </div>
          <% }) %>
        </div>
      <% } %>
    <% }) %>

    <% if (regions.length === 0 && (!query || query.trim() === '')) { %>
      <div class="card" style="text-align: center;">
        <p>ไม่พบข้อมูลสภาพอากาศ</p>
      </div>
    <% } else if (regions.length === 0 && query && query.trim() !== '') { %>
      <div class="card" style="text-align: center;">
        <p>ไม่พบข้อมูลสำหรับจังหวัด "<%= query %>"</p>
      </div>
    <% } %>
  </div>
</body>
</html>
